input:
  format: json
  config:
    connector_mapping: ./mapping_shopify_delete.yaml

producer:
  schema_file: ../../schemas/stripe.shopify_delete/stripe.shopify_delete-1.0.0.yaml
  policy_file: ./policies_shopify_delete.yaml

output:
  archive_type: noop
  archive_config: ../archive/noop.yaml
  client_type: noop
  client_config: ../clients/noop.yaml

observability:
  labels:
    environment: local
    pipeline: shopify-catalog-delete
  logging:
    level: INFO
    flood_control_config: {}
  telemetry:
    provider: none
