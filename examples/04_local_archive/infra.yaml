input:
  format: jsonl
  config:
    connector_mapping: ./mapping.yaml

producer:
  schema_file: ./stripe.product_feed-1.0.0.yaml
  policy_file: ./policies.yaml

output:
  archive_type: local
  archive_config: ./archive.local.yaml
  client_type: noop
  client_config: ./client.noop.yaml
