input:
  format: jsonl
  config:
    connector_mapping: ../../mappings/generic_catalog.yaml

producer:
  schema_file: ../../../../schemas/stripe.product_feed/stripe.product_feed-1.0.0.yaml
  policy_file: ../../../../config/policies.yaml

output:
  archive_type: local
  archive_config: ../../config/archive/local.yaml
  client_type: noop
  client_config: ../../../../config/clients/noop.yaml

observability:
  labels:
    environment: test
    pipeline: matrix-archiver-local
  logging:
    level: INFO
    flood_control_config: {}
  telemetry:
    provider: none