input:
  format: jsonl
  config:
    connector_mapping: ../../mappings/generic_catalog.yaml

producer:
  schema_file: ../../../../schemas/stripe.product_feed/stripe.product_feed-1.0.0.yaml
  policy_file: ../../../../config/policies.yaml

output:
  archive_type: noop
  archive_config: ../../../../config/archive/noop.yaml
  client_type: http
  client_config: ../../config/clients/http.yaml

observability:
  labels:
    environment: test
    pipeline: matrix-client-http
  logging:
    level: INFO
    flood_control_config: {}
  telemetry:
    provider: none