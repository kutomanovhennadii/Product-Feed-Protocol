input:
  format: jsonl
  config:
    connector_mapping: ../../mappings/generic_catalog.yaml

producer:
  schema_file: ../../../../schemas/openai.product_feed/openai.product_feed-1.0.0.yaml
  policy_file: ../../../../config/policies.yaml

output:
  archive_type: noop
  archive_config: ../../../../config/archive/noop.yaml
  client_type: noop
  client_config: ../../../../config/clients/noop.yaml

observability:
  labels:
    environment: test
    pipeline: matrix-schema-openai-product-feed
  logging:
    level: INFO
    flood_control_config: {}
  telemetry:
    provider: none