input:
  format: json
  config:
    connector_mapping: ../../../../config/shopify_delete/mapping_shopify_delete.yaml

producer:
  schema_file: ../../../../schemas/stripe.shopify_delete/stripe.shopify_delete-1.0.0.yaml
  policy_file: ../../../../config/shopify_delete/policies_shopify_delete.yaml

output:
  archive_type: noop
  archive_config: ../../../../config/archive/noop.yaml
  client_type: noop
  client_config: ../../../../config/clients/noop.yaml

observability:
  labels:
    environment: test
    pipeline: matrix-schema-shopify-delete
  logging:
    level: INFO
    flood_control_config: {}
  telemetry:
    provider: none