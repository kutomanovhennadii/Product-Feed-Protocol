input:
  format: json
  config:
    connector_mapping: ../../../../config/shopify_inventory/mapping_shopify_inventory.yaml

producer:
  schema_file: ../../../../schemas/stripe.shopify_inventory/stripe.shopify_inventory-1.0.0.yaml
  policy_file: ../../../../config/shopify_inventory/policies_shopify_inventory.yaml

output:
  archive_type: noop
  archive_config: ../../../../config/archive/noop.yaml
  client_type: noop
  client_config: ../../../../config/clients/noop.yaml

observability:
  labels:
    environment: test
    pipeline: matrix-schema-shopify-inventory
  logging:
    level: INFO
    flood_control_config: {}
  telemetry:
    provider: none