header:
  protocol_id: "stripe.product_feed"
  schema_version: "1.0.0"
  artifact_profile: "catalog_snapshot"
  title: "Fixture schema for pipeline_manifest_provider integration test"
  source_protocol:
    provider: "stripe"
    url: "https://example.com"
    revision: "r1"
    retrieved_at: "2026-03-08"

input:
  um_contract: "um.v1"

output:
  output_kind: "csv_row"
  writer_id: "csv"
  artifact:
    content_type: "text/csv"
    file_ext: ".csv"

mapping:
  output_kind: "csv_row"
  fields:
    id:
      source:
        path: "item_id"
        required: true

validation:
  rules: []
